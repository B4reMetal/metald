services:
  metald:
    image: metald:dev
    build: ..
    restart: unless-stopped
    read_only: true          # the only writable place is the mount below
    volumes:
      # config.yml, plugins/ for your own plugins, data/ for runtime state
      - ./config:/config
